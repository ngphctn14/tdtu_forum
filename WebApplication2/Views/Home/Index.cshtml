@{
    ViewBag.Title = "TDTU Forum";
    Layout = "~/Views/Shared/_Layout.cshtml";

}

@{ Html.RenderAction("getHomeWelcome", "Home");}

<section class="main-content920">
    <div class="container">
        <div class="row">
            <div class="col-md-9">
                @{ Html.RenderAction("getPostsForEachCategory", "Category");}
            </div>
            <aside class="col-md-3 sidebar97239">
                <div class="categori-part329">
                    <h4>Categories</h4>
                    <ul>
                        @{Html.RenderAction("getDetails", "Category");}
                    </ul>
                </div>
                @{ Html.RenderAction("loginPartialView", "Login");}
                @{Html.RenderAction("get15RecentPosts", "Post");}
            </aside>
        </div>
    </div>
</section>
